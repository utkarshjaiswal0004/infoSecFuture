<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification Error</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
        }
        h1 {
            color: red;
        }
        #countdownValue {

            color: red;
        }
        #countdown {
            font-size: 24px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Oops, something went wrong!</h1>
    <p>An error occurred while verifying your email.</p>
    <p><%= error %></p>
    
    <div id="countdown">Closing in <span id="countdownValue">8</span> seconds...</div>

    <script>
        // Countdown timer
        let countdown = 8;
        const countdownElement = document.getElementById('countdownValue');

        function updateCountdown() {
            countdown--;
            countdownElement.textContent = `${countdown}`;
            if (countdown === 0) {

                window.location.href = `https://bucolic-paprenjak-299acc.netlify.app`;
            }
        }

        const countdownInterval = setInterval(updateCountdown, 1000);

        setTimeout(() => {
            clearInterval(countdownInterval);
            countdownElement.textContent = 'Closing...';
        }, 10000); // 10 seconds
    </script>
</body>
</html>
